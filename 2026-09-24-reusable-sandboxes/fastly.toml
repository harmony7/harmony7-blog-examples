# This file describes a Fastly Compute package. To learn more visit:
# https://www.fastly.com/documentation/reference/compute/fastly-toml

authors = [""]
description = "Reusable sandboxes: one sandbox accepting more than one request"
language = "other"
manifest_version = 3
name = "reusable-sandboxes"
service_id = ""

[scripts]
  build = "cargo build --target wasm32-wasip2 && mkdir -p bin && cp target/wasm32-wasip2/debug/reusable_sandboxes.wasm bin/main.wasm"
