# This file describes a Fastly Compute package. To learn more visit:
# https://www.fastly.com/documentation/reference/compute/fastly-toml

authors = [""]
description = "Reading downstream TLS, bot, and VPN/proxy signals from http-downstream"
language = "other"
manifest_version = 3
name = "http-downstream-tls-bots"
service_id = ""

[scripts]
  build = "cargo build --target wasm32-wasip2 && mkdir -p bin && cp target/wasm32-wasip2/debug/http_downstream_tls_bots.wasm bin/main.wasm"
