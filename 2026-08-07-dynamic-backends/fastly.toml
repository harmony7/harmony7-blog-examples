# This file describes a Fastly Compute package. To learn more visit:
# https://www.fastly.com/documentation/reference/compute/fastly-toml

authors = [""]
description = "Registering a backend at runtime instead of declaring it ahead of time"
language = "other"
manifest_version = 3
name = "dynamic-backends"
service_id = ""

[scripts]
  build = "cargo build --target wasm32-wasip2 && mkdir -p bin && cp target/wasm32-wasip2/debug/dynamic_backends.wasm bin/main.wasm"
